#include "ot_context.h" 

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <arpa/inet.h>
#include <assert.h>

void ot_cli_auth(ot_cli_ctx* ctx)
{
  assert("ot_cli_auth not yet implemented" && false);
  return;
}

void ot_cli_renew(ot_cli_ctx* ctx)
{
  assert("ot_cli_renew not yet implemented" && false);
  return;
}

void ot_cli_pull(ot_cli_ctx ctx, const char* uname, char* dest_psk)
{
  assert("ot_cli_pull not yet implemented" && false);
  return;
}


